
#include<stdio.h>
//Écrivez un programme pour vérifier si le nombre donné est positif, négatif ou nul. Si le nombre est inférieur à zéro, alors le nombre est négatif et si le nombre est supérieur à zéro, alors le nombre est positif.
 //Si les deux conditions sont fausses, le nombre est égal à zéro.
 int main(){
    int nombre;
    printf("saisir le nombre");
    scanf("%d",&nombre);
     if (nombre>0)
    printf("le nombre est positif ");
     if else (nombre<0)
    printf("le nombre est negatif");
    else
    printf("le nombre est egal a zero ");
 }